<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="007-create-appointments" author="Dev3">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="appointments"/>
            </not>
        </preConditions>

        <createTable tableName="appointments">

            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="patient_id" type="UUID">
                <constraints nullable="false" foreignKeyName="fk_appointment_patient" references="users(id)"/>
            </column>

            <column name="doctor_id" type="UUID">
                <constraints nullable="false" foreignKeyName="fk_appointment_doctor" references="users(id)"/>
            </column>

            <column name="appointment_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="appointment_time" type="TIME">
                <constraints nullable="false"/>
            </column>

            <column name="slot" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="type" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="reason" type="VARCHAR(255)"/>

            <column name="created_at" type="TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP"/>

        </createTable>

        <createIndex indexName="idx_appointment_patient" tableName="appointments">
            <column name="patient_id"/>
        </createIndex>

        <createIndex indexName="idx_appointment_doctor" tableName="appointments">
            <column name="doctor_id"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>